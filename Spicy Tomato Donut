<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spicy Tomato Donut | Home of the Bold Glaze</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #D32F2F;
            --black: #1a1a1a;
            --white: #ffffff;
            --gray: #f9f9f9;
        }

        * { box-sizing: border-box; transition: all 0.3s ease; }

        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            overflow-x: hidden;
            color: var(--black);
            background: var(--white);
        }

        /* Navigation */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 5%;
            background: var(--white);
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid #eee;
        }

        .logo { font-size: 1.4rem; font-weight: 900; letter-spacing: 3px; color: var(--primary); text-decoration: none; }

        nav a { text-decoration: none; color: var(--black); margin-left: 25px; font-size: 0.8rem; letter-spacing: 1px; text-transform: uppercase; }

        .cart-icon { cursor: pointer; position: relative; font-size: 1.2rem; margin-left: 25px; }
        .cart-count {
            position: absolute; top: -10px; right: -10px;
            background: var(--primary); color: white;
            font-size: 0.7rem; padding: 2px 6px; border-radius: 50%;
        }

        /* Hero Section */
        .hero {
            display: flex; height: 90vh; align-items: center; background: var(--gray);
        }
        .hero-left { flex: 1; padding: 0 8%; }
        .hero-right { 
            flex: 1; height: 100%; 
            background: url('https://images.unsplash.com/photo-1551024601-bec78aea704b?auto=format&fit=crop&w=1000&q=80') center/cover;
        }
        .hero-left h1 { font-size: clamp(3rem, 8vw, 5rem); line-height: 0.9; margin: 0; font-weight: 900; }

        /* Shop Section */
        .shop-section { padding: 80px 5%; text-align: center; }
        .product-grid {
            display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 40px; margin-top: 50px;
        }
        .product-card { border: 1px solid #eee; padding-bottom: 25px; background: white; }
        .product-card img { width: 100%; height: 320px; object-fit: cover; }
        .product-card h3 { font-size: 1.1rem; margin: 20px 0 5px; letter-spacing: 1px; }
        .price-tag { color: var(--primary); font-weight: bold; margin-bottom: 15px; display: block; }
        
        .add-btn {
            background: var(--black); color: white; border: none;
            padding: 12px 25px; cursor: pointer;
            text-transform: uppercase; font-size: 0.75rem; letter-spacing: 2px;
        }
        .add-btn:hover { background: var(--primary); transform: translateY(-2px); }

        /* Cart Drawer */
        #cart-drawer {
            position: fixed; top: 0; right: -450px; width: 400px; height: 100%;
            background: white; box-shadow: -10px 0 30px rgba(0,0,0,0.1);
            z-index: 2000; padding: 40px 30px; display: flex; flex-direction: column;
        }
        #cart-drawer.active { right: 0; }
        .cart-header { display: flex; justify-content: space-between; border-bottom: 1px solid #eee; padding-bottom: 20px; align-items: center; }
        .cart-items { flex: 1; overflow-y: auto; margin-top: 20px; }
        .cart-item { display: flex; justify-content: space-between; margin-bottom: 20px; font-size: 0.95rem; border-bottom: 1px solid #f9f9f9; padding-bottom: 10px; }
        .cart-total { border-top: 2px solid var(--black); padding-top: 25px; margin-top: 10px; }

        /* Contact Section */
        .contact-container { display: flex; padding: 100px 5%; background: var(--white); flex-wrap: wrap; }
        .contact-left { flex: 1; min-width: 300px; }
        .contact-right { flex: 1; min-width: 300px; }
        input, textarea { width: 100%; border: none; border-bottom: 1px solid #000; padding: 15px 0; margin-bottom: 25px; outline: none; font-size: 1rem; }

        footer { padding: 40px 5%; text-align: center; font-size: 0.75rem; border-top: 1px solid #eee; letter-spacing: 1px; color: #777; }

        @media (max-width: 768px) {
            .hero { flex-direction: column; }
            .hero-right { height: 350px; width: 100%; }
            #cart-drawer { width: 100%; right: -100%; }
            .hero-left h1 { font-size: 3.5rem; padding: 50px 0; }
        }
    </style>
</head>
<body>

    <header>
        <a href="#" class="logo">SPICY DONUT</a>
        <nav>
            <a href="#home">Home</a>
            <a href="#shop">Shop</a>
            <a href="#contact">Contact</a>
            <span class="cart-icon" onclick="toggleCart()">
                <i class="fas fa-shopping-bag"></i>
                <span class="cart-count" id="count">0</span>
            </span>
        </nav>
    </header>

    <div id="home" class="hero">
        <div class="hero-left">
            <h1>HOT<br>FRESH<br>BOLD.</h1>
            <p style="margin: 30px 0; max-width: 400px;">Experience the savory Filipino revolution. The 2026 Spicy Tomato Donut is officially here.</p>
            <a href="#shop" class="add-btn" style="padding: 18px 40px; text-decoration: none;">Shop Now</a>
        </div>
        <div class="hero-right"></div>
    </div>

    <section id="shop" class="shop-section">
        <h2 style="font-size: 2.5rem; letter-spacing: 5px;">THE MENU</h2>
        <div class="product-grid">
            <div class="product-card">
                <img src="https://images.unsplash.com/photo-1551024601-bec78aea704b?w=500" alt="The OG Spicy">
                <h3>THE OG SPICY</h3>
                <span class="price-tag">₱65.00</span>
                <button class="add-btn" onclick="addToCart('The OG Spicy', 65.00)">Add to Cart</button>
            </div>
            <div class="product-card">
                <img src="https://images.unsplash.com/photo-1527515637462-cff94eecc1ac?w=500" alt="Fire Cracker">
                <h3>FIRE CRACKER</h3>
                <span class="price-tag">₱75.00</span>
                <button class="add-btn" onclick="addToCart('Fire Cracker', 75.00)">Add to Cart</button>
            </div>
            <div class="product-card">
                <img src="https://images.unsplash.com/photo-1615837135355-321264024388?w=500" alt="Sweet Tomato Glaze">
                <h3>SWEET TOMATO GLAZE</h3>
                <span class="price-tag">₱60.00</span>
                <button class="add-btn" onclick="addToCart('Tomato Glaze', 60.00)">Add to Cart</button>
            </div>
        </div>
    </section>

    <div id="cart-drawer">
        <div class="cart-header">
            <h3 style="margin: 0; letter-spacing: 2px;">MY CART</h3>
            <i class="fas fa-times" onclick="toggleCart()" style="cursor: pointer; font-size: 1.5rem;"></i>
        </div>
        <div class="cart-items" id="cart-list">
            <p style="color: #999; text-align: center; margin-top: 50px;">Your cart is empty.</p>
        </div>
        <div class="cart-total">
            <div style="display: flex; justify-content: space-between; font-size: 1.2rem; font-weight: 900;">
                <span>TOTAL</span>
                <span>₱<span id="total-price">0.00</span></span>
            </div><button onclick="cart=[]; updateCart();" style="width: 100%; background: none; border: 1px solid #eee; margin-top: 10px; padding: 10px; font-size: 0.7rem; cursor: pointer;">
    EMPTY CART
</button>

            <button class="add-btn" style="width: 100%; margin-top: 25px; padding: 20px;" onclick="sendToWhatsApp()">
    Order via WhatsApp
</button>

        </div>
    </div>

    <div class="contact-right" style="display: flex; align-items: center; justify-content: center; min-height: 300px;">
    <div style="text-align: center; width: 100%;">
        <p style="margin-bottom: 20px; letter-spacing: 2px; font-size: 0.8rem; color: #666;">
            HAVE QUESTIONS OR BULK ORDERS?
        </p>
        <button onclick="sendGeneralInquiry()" class="add-btn" style="width: 100%; padding: 25px; font-size: 1rem;">
            <i class="fab fa-whatsapp" style="margin-right: 10px;"></i> 
            CHAT ON WHATSAPP
        </button>
    <footer>
        <p>© 2026 BY SPICY TOMATO DONUT. CRAFTED IN SABLAYAN.</p>
    </footer>

    <script>function sendToWhatsApp() {
    if (cart.length === 0) {
        alert("Your cart is empty! Add some bold flavors first.");
        return;
    }

    // 1. Set your business phone number (Format: 639XXXXXXXXX)
    const phoneNumber = "639261464801"; 

    // 2. Build the message string
    let message = "🔥 *NEW ORDER - SPICY TOMATO DONUT* 🔥%0A%0A";
    
    cart.forEach((item, index) => {
        message += `${index + 1}. *${item.name}* - ₱${item.price.toFixed(2)}%0A`;
    });

    const total = document.getElementById('total-price').innerText;
    message += `%0A💰 *Total: ₱${total}*%0A%0A--------------------------%0A📍 *Delivery Details:*%0A(Please type your address/landmark here)`;

    // 3. Open WhatsApp with the pre-filled message
    const whatsappUrl = `https://wa.me/${phoneNumber}?text=${message}`;
    window.open(whatsappUrl, '_blank');
}

        let cart = [];
        let total = 0;

        function toggleCart() {
            document.getElementById('cart-drawer').classList.toggle('active');
        }

        function addToCart(name, price) {
            cart.push({name, price});
            updateCart();
            // Automatically open cart when adding first item
            if(!document.getElementById('cart-drawer').classList.contains('active')) {
                toggleCart();
            }
        }

        function updateCart() {
            const list = document.getElementById('cart-list');
            const count = document.getElementById('count');
            const totalDisplay = document.getElementById('total-price');
            
            list.innerHTML = '';
            total = 0;

            cart.forEach((item) => {
                total += item.price;
                list.innerHTML += `
                
                    <div class="cart-item">
                        <span>${item.name}</span>
                        <span style="font-weight: bold;">₱${item.price.toFixed(2)}</span>
                    </div>
                `;
            });

            if(cart.length === 0) {
                list.innerHTML = '<p style="color: #999; text-align: center; margin-top: 50px;">Your cart is empty.</p>';
            }

            count.innerText = cart.length;
            totalDisplay.innerText = total.toLocaleString('en-PH', { minimumFractionDigits: 2 });
        }function sendGeneralInquiry() {
    // REPLACE THE NUMBER BELOW WITH YOUR ACTUAL NUMBER
    const phoneNumber = "639261464801"; 
    
    const message = "Hi! I'm checking your website and I have a question about the Spicy Tomato Donuts. 🌶️🍅";
    
    // This creates the link and opens WhatsApp
    const whatsappUrl = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;
    
    window.open(whatsappUrl, '_blank');
}

    </script>

</body>
</html>
